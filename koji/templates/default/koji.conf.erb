[koji]

;configuration for koji cli tool

;url of XMLRPC server
server = <%= node['koji_koji.conf_server'] %>

;url of web interface
weburl = <%= node['koji_koji.conf_weburl'] %>

;url of package download site
;topurl = http://kojipkgs.fedoraproject.org/
topurl = <%= node['koji_koji.conf_topurl'] %>

;path to the koji top directory
topdir = <%= node['koji_koji.conf_topdir'] %>

;configuration for Kerberos authentication

;the service name of the principal being used by the hub
;krbservice = host

;configuration for SSL authentication

;client certificate
cert = <%= node['koji_koji.conf_cert'] %>

;certificate of the CA that issued the client certificate
ca = <%= node['koji_koji.conf_ca'] %>

;certificate of the CA that issued the HTTP server certificate
serverca = <%= node['koji_koji.conf_serverca'] %>
